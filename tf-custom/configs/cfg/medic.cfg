exec common

// --- Faking / Masking
bind q "voicemenu 2 7; say_team BUT WHAT IF THE UBER HAS US?"
bind r "voicemenu 1 7; say_team FAKED LIKE ANYTHING CRITICISING TRUMP"

// --- Medic radar
alias autocall_default "hud_medicautocallersthreshold "80""
alias autocall_all "hud_medicautocallersthreshold "150""
alias +radar autocall_all
alias -radar autocall_default

// --- Firing modes
alias fm_default "-attack; alias +fire +attack; alias -fire -attack"
alias fm_medigun "+attack; alias +fire -attack; alias -fire +attack"

// --- Popping shit
alias +pop "+attack2; say_team UBER PRANK (GONE WRONG) (GONE SEXUAL) (PLAYERS DROPPED)"
alias -pop "-attack2"

// --- Weapon logic
alias crossbow "slot1; bind mouse2 +attack2; fm_default"
alias medigun "slot2; bind mouse2 +pop; fm_medigun"
alias saw "slot3; bind mouse2 +attack2; fm_default"

bind mwheelup crossbow
bind mwheeldown medigun
bind mouse1 +fire
bind mouse3 +radar
alias +sideButton saw

exec mouse

medigun
